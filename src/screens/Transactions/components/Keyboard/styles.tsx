import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  buttonKeyContainer: {
    flex: 1,
    flexDirection: 'row',
    justifyContent: 'center',
    alignItems: 'center',
    borderRadius: 10,
    margin: 6,
  },
  buttonKeyText: {
    fontSize: 25,
    fontWeight: '700',
  },
})
